# Game Assets

Place imported runtime assets under the matching type directory and editable source files under `source-art/`. Expected binary formats are tracked with Git LFS. Do not add an AI-assisted release asset without a completed record in `data/provenance/ai-assets.csv`.
