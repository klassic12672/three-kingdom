# Character, Family, and Household Foundation

SP-04A through SP-04F0 have accepted exact-SHA hosted macOS arm64/Windows x64 evidence at the revisions linked in the package sections below. SP-04F1 is locally implemented and verified but awaits exact-SHA hosted evidence. SP-04 remains Active and is not complete.

## Package boundary

The current character packages provide versioned character, family, household, identity, relationship, memory, retinue, patronage, recommendation, employment-history, personal-wealth, opaque character-estate-holding, marriage, primary-guardianship, active-pregnancy, and source-backed education-attainment state; deterministic authoritative queries; v1/v2 authored-content loading; registered career/social/resource/political-marriage/adult-romance/condition/household/legal-adoption/primary-guardianship establishment/end/replacement, adult union-linked pregnancy registration, due/overdue pregnancy birth resolution with generated-child insertion, primary-guardian ability attainment, and local-candidate restricted public-death resolution; coercive political-union consequences; deterministic coming-of-age events with atomic primary-guardianship closure; and save persistence. Voluntary-romance relationship effects, broader guardian authority behavior, conception or birth scheduling, automatic name generation, pregnancy loss or twins, education enrollment/curricula/progress/scheduling/levels, aptitude or trait mutation, ability removal/decay, household/family/faction/court treasuries, debt, currency and commodities, income and spending, physical/economic estate integration, broader death/career/retinue/household lifecycle policy, inheritance, succession, battle integration, historical rosters, shipped character content, and UI remain deferred.

## Contracts and queries

Runtime character snapshot, state, profile/query, and `simulation.characters` contracts are version 3 in `Simulation.Core`; identity, character, family, and household definitions remain version 2:

| Contract | Purpose |
|---|---|
| `CharacterIdentityDefinition` | Stable ability, aptitude, trait, flaw, ambition, or reputation ID plus localization key |
| `CharacterDefinition` | Stable identity, retained primary `nameKey`, structured primary/courtesy names, content origin/lineage, optional culture/origin, birth date, and typed identity references |
| `FamilyDefinition`, `HouseholdDefinition` | Stable localized family and household identities |
| `CharacterState` | Retained parent IDs, matching typed biological/legal-adoptive/legacy parent links, vital/health/incapacity/custody condition, and immutable education attainments |
| `FamilyState` | Family membership |
| `HouseholdState` | Household membership and head |
| `CharacterWorldSnapshot` | Canonical authoritative subsystem snapshot |
| `AuthoritativeCharacterProfile`, `AuthoritativeHouseholdView` | Defensive, unfiltered simulation read models |

`WorldState.Characters` is the authoritative `CharacterWorldState`; `IWorldQuery.Characters` exposes it through `IAuthoritativeCharacterWorldQuery`. These results are deliberately named as authoritative and unfiltered. The SP-04 names `CharacterProfile` and `HouseholdView` remain reserved for later observer-aware, player-knowledge-filtered queries in `Game.Application`.

Authoritative profiles return the structured descriptor, condition, typed parent/child links, retained parent/child ID lists, nullable family and household membership, immutable education records, effective abilities, and age derived from the campaign date. Effective abilities are the canonical union of the immutable definition baseline and acquired education abilities; definitions never mutate. Age calculation uses `CampaignDate` only and changes deterministically on the birthday. Family and household membership are separate: a person may share one without sharing the other. Every nested collection is a defensive copy.

## Determinism and validation

Top-level definition and state input order is normalized by stable `EntityId` order. Nested identity, flaw, parent-link, provenance-lineage, source, family-member, and household-member lists must already be unique and ordinal-canonical in authoritative state. Captured snapshots and query results are defensive copies.

Construction rejects:

- unsupported snapshot, definition, or state versions;
- invalid or duplicate global definition IDs and duplicate states;
- missing or mistyped ability, aptitude, trait, flaw, ambition, and reputation definitions;
- null/mismatched structured names, invalid origin/classification/pack lineage, and invalid culture/origin IDs;
- null, mismatched, duplicate, non-canonical, or invalid typed parent links;
- invalid vital/health/incapacity/custody combinations, self-custody, and missing custodians;
- null, oversized, duplicate, non-canonical, future, or incorrectly sourced education attainments;
- education records with a mismatched ward, missing teacher, non-ability target, teacher without the baseline ability, adult-at-resolution ward, duplicate baseline/acquired ability, invalid derived ID, or missing retained guardianship source;
- dangling, self-referential, cyclic, or chronologically impossible parentage;
- characters born after the current campaign date;
- dangling or duplicate family and household members;
- membership in multiple families or multiple households;
- household heads who are not members; and
- non-canonical nested ID ordering.

## Content loading and localization

`CharacterContentLoader` reads resolved registry records of these types:

- `character_world`
- `character_definition`
- `family_definition`
- `household_definition`
- `character_identity_definition`

The published content-record schema defines closed version-1 and version-2 payloads for all five types. Both remain accepted and normalize to runtime v3 with empty education attainments; definitions remain v2. Version 1 parent IDs become `UnspecifiedLegacy`, never biological. Version 2 adds courtesy names, authored/custom origin, optional culture/location references, flaws, typed parent links, condition, and custodian references. Runtime-v3 world/state versions and `educationAttainments` are rejected at the authored boundary. The loader requires unique canonical per-kind IDs, exact agreement between consumed references and `data.references`, exact name-key declaration in `localizationKeys`, and non-empty `ko-KR` and `en-US` text for primary and courtesy names. Non-fictional v2 records require authored origin and sources; fictional v2 records require custom origin.

Normalized records retain the original owning pack and a canonical list of applied override packs. Every typed record authored or overridden by a candidate pack is validated even when no world selects it; resolved-world graph validation separately checks selected types, references, state, condition, and kinship. Invalid candidates roll back record data and lineage together. Culture and origin currently require loaded stable records but do not invent a separate C0 culture/location subsystem. The pre-existing generic `character` record, built-in pack, and shipped content are unchanged.

Registry construction validates age-independent character-world structure using the latest selected birth date. The real campaign start date remains authoritative when `WorldState` is created, because `character_world` does not itself own a scenario date.

Automated coverage uses only fictional, test-owned content: four characters, one family, two independent households, typed parentage/custody, all six identity-definition kinds, structured/courtesy names, and Korean/English localization. No historical assertion or shipped content was added.

## Persistence and compatibility

`WorldSnapshot` contract version remains 1 through additive subsystem properties. Newly captured state registers `simulation.characters@3`, `simulation.relationships@2`, `simulation.character_careers@1`, `simulation.character_resources@1`, `simulation.character_estate_holdings@1`, `simulation.character_marriages@2`, `simulation.character_guardianships@1`, and `simulation.character_pregnancies@1`; `SimulationChecksum` canonicalizes every character descriptor, condition, typed-link, education attainment, relationship, memory-source, career, wealth-account, ledger-entry, folded-history, estate, marriage, guardianship, and active-pregnancy field.

Current `SaveEnvelope` schema 20 requires complete character-v3 state with non-null attainment arrays, relationship-v2, character-marriage-v2, and complete v1 career, resource, estate-holding, guardianship, and active-pregnancy snapshots with their exact system registrations. Omission, null, partial state, unsupported nested versions, and a missing, duplicate, or incompatible system version are rejected. The authenticated 18→19 E5 history remains unchanged. The 19→20 migration authenticates the exact E5 fixture, advances only runtime character snapshot/state/system tags, inserts empty attainments into snapshot and retained E5 diagnostic child states, preserves definition versions and all prior values/source bytes, and computes the current checksum. Historical projection removes attainments and returns runtime v3 to the exact schema-19 v2 shape. Schema 19 rejects E6 vocabulary, properties including explicit null, and v3 version injection. The complete 1→20 chain remains forward-only; schema-1/2 field compatibility remains explicitly unverified. Legacy standalone snapshots may omit a later subsystem only when the complete omitted state is valid and empty; restored captures write current versions.

## Verification

Focused commands:

```bash
dotnet test tests/Simulation.Core.Tests/Simulation.Core.Tests.csproj -c Release --filter FullyQualifiedName~Character
dotnet test tests/Simulation.Core.Tests/Simulation.Core.Tests.csproj -c Release --filter FullyQualifiedName~Save
dotnet test tests/Game.Content.Tests/Game.Content.Tests.csproj -c Release --filter FullyQualifiedName~Character
```

Performance evidence was collected on 2026-07-14 from an uncommitted local working tree based on `4e6e83cb5a8f70b33e109d84782ef16681bd6e20`, using macOS 26.5.1 build 25F80 on arm64 and .NET SDK 10.0.301:

```bash
dotnet test tests/Simulation.Core.Tests/Simulation.Core.Tests.csproj -c Release --filter FullyQualifiedName~CharacterWorldStateConstructsAndQueriesOneThousandCharacters --logger "console;verbosity=detailed"
```

The run constructed 1,000 characters and one household in 23.608 ms, then performed 1,000 profile lookups and one household lookup in 5.674 ms. The test asserts counts and correctness, not wall-clock thresholds. The full relationship/memory campaign-turn budget remains unchecked because those systems are outside SP-04A.

The pre-remediation candidate remains disqualified because `GeographyTests.PathfindingUsesRoutesOnlyAndMeetsInteractionBudget` later failed its unchanged `<50 ms` threshold; its earlier audit is not relabeled as passing. Corrected-tree evidence collected on 2026-07-15 passed:

- `./scripts/validate.sh`: 1,295 records and 2,820 translations valid; registry checksum `b04754a678bbb971045e4b2d602df5bf5c48fe26fc606b595449391e54d6b2a0` (unchanged because the fixture is test-owned);
- 39 `SaveStoreTests`, including three schema-specific explicit-null recovery and byte-preservation cases;
- focused closeout: 36 Simulation.Core character, 29 Game.Content character, 3 published-schema, and 1 architecture-boundary tests passed;
- `./scripts/test.sh Release`: 107 Simulation.Core, 66 Game.Content, and 18 repository tests passed with zero failures;
- the ten-year/1,000-entity synthetic soak with the new authoritative subsystem: checksum `cc6cba9f2b5408921fdbcd15a8d5494ca2351e73d7f3052f16702a09639af702`;
- `git diff --check`; and
- `git lfs fsck`.

These are local macOS results from an uncommitted tree, not clean-checkout, same-revision hosted, or cross-platform evidence. Nothing was staged or committed. Subsequent exact-SHA hosted macOS arm64/Windows x64 verification passed at `eaa3aaf3a0687a231d2a3441e5be4954e905e9ea`; that later evidence does not relabel the historical working-tree results. Physical Windows packaged-save evidence remains an M4 gate, and production signing remains an SP-15 gate. No push or hosted action was authorized.

## SP-04C0 verification

The current uncommitted C0 tree passed `./scripts/validate.sh` and `./scripts/test.sh Release`: 204 Simulation.Core, 71 Game.Content, 6 Game.Application, and 18 repository tests. Focused coverage includes 41 character tests and 82 save/recovery tests. Validation retained 1,295 records, 2,820 translations, and registry checksum `b04754a678bbb971045e4b2d602df5bf5c48fe26fc606b595449391e54d6b2a0`. The schema-5 frozen checksum is `4ef74d59d48b7415cc86a40eca98ca3ac3fdafe5c0a5047bdb3b1ff3d5f3ea14`; the current ten-year/1,000-entity soak checksum is `37504979fcaa25789cc9e12af7084c351d115c1195054e062ca7f7ea6ba943dd`.

On local Apple Silicon macOS, the v2 fixture constructed 1,000 characters and one household in 31.241 ms, then performed 1,000 profile lookups and one household lookup in 6.811 ms. The test asserts shape and correctness, not a wall-clock threshold. `git diff --check` and `git lfs fsck` also passed. Those measurements remain local working-tree evidence. Accepted revision `7d4612d21784ceebbcd574ea00231785b9408036` subsequently passed hosted macOS arm64 and Windows x64 validation, complete tests, import, native export, automated smoke, manifest inspection, and artifact verification. Physical Windows, signing, Steam, and release readiness remain outside C0. The complete C0 verification matrix and package limits are recorded in the [SP-04 plan](plans/SP-04-characters-family-marriage-succession.md).

## SP-04B-L relationship and memory kernel

SP-04B-L adds independent directional affection, trust, respect, attraction, obligation, fear, resentment, rivalry, and compatibility state without a universal loyalty score. `RelationshipActionCommandPayload` uses the issuing character as the subject and names one different target; `RelationshipActionResolvedEventPayload` is the only persistent mutation path. Nonzero attraction changes require both participants to be at least 18 on the exact resolution date. Reciprocal changes require a separate command.

Every accepted action creates one deterministic consequential memory containing subject, target, canonical witnesses, authoritative date/turn, opaque meaning ID, initial severity, publicity, derived decay, applied impact, and source event. Private, Participants, Witnessed, and Public visibility is enforced by `RelationshipSummaryQuery` in `Game.Application`. Subjects receive exact detailed dimensions and their active retained memories; other existing observers receive only visible active memories and no exact dimensions. Archived and distant summaries are subject-only.

History is bounded to 64 detailed links and 128 archived summaries per subject, 16 detailed memories per link, and one fixed-size distant aggregate. Retention uses effective severity/recorded turn/ID for memories and recorded importance/last-change turn/ID for links; evicted state folds into checked fixed-size summaries. Relationship IDs hash the ordered subject/target sequence, and memory IDs hash the resolution date/command ID using exact versioned UTF-8 framing. Relationship source-event IDs frame both the authoritative date and command identity, so reusing a command ID on a later date cannot alias the earlier causal event.

`RelationshipWorldSnapshot` version 1 is a separate default-empty `WorldSnapshot` subsystem registered as `simulation.relationships@1`. Save schema 5 includes it in canonical checksums. Schema 4 is authenticated before the explicit 4→5 migration injects empty relationship state and recomputes the checksum; geography, character data, source files, and autosave candidates remain unchanged.

SP-04B-L local verification and exact-SHA hosted macOS arm64/Windows x64 verification passed. The hosted evidence establishes the unchanged suites, deterministic assertions, import/export/smoke path, and artifact manifests on both hosted release-target architectures; it does not convert the recorded Apple Silicon wall-clock measurements into hosted performance evidence. The package-specific matrix, focused commands, raw local performance measurements, checksums, and limitations are recorded in the [SP-04 plan](plans/SP-04-characters-family-marriage-succession.md). Marriage, romance, household expansion, birth, death, inheritance, succession, content, UI, battle integration, and platform behavior remain deferred.

## SP-04C1 career and social-action kernel

SP-04C1 adds `CharacterCareerWorldState` as a deterministic, checksum-covered, default-empty subsystem registered as `simulation.character_careers@1`. It distinguishes a character's personal retinue, patronage bonds, recommendations, and employment tenures. Employment principals may be a character or household; this does not infer faction, office, title, or resource ownership. Authoritative career queries return defensive canonical proposal, service, recommendation, and folded-history records.

`character_action.v1` is the sole registered command envelope for eleven explicit action variants: invite/respond/leave retinue, offer/respond/end patronage, recommend, offer/respond/end employment, and withdraw a proposal. `character_action_resolved.v1` is the persistent mutation event. The issuer must be an existing living, conscious, non-incapacitated character at submission and resolution. Custody is action-specific: it prevents initiating proposals and recommendations and prevents captive recipients from accepting retinue or employment service, while still allowing the bounded patronage response path. Offers are revalidated when resolved; a once-valid response becomes an explicit `Invalidated` outcome if either participant or its referenced principal is no longer eligible. A resolved event is replanned and compared against its command, deterministic IDs, authoritative date/turn, and complete outcome before mutation.

One character action may carry canonical, explicitly authored relationship consequences. Career and relationship candidate states are both prepared before either is committed, so a malformed or overflowed relationship consequence cannot leave partial career state. Generic memories use the character-action event plus zero-based consequence index as their causal identity and may record zero-dimensional impacts; relationship actions retain their legacy resolution-date/command memory IDs. Relationship snapshot and application query contracts are therefore version 2, while migrated v1 memory IDs remain stable and carry an explicit legacy identity scheme.

Career history and event workload are bounded rather than silently discarded. Active proposals are capped at 8 per recipient; active retinue memberships at 64 per retinue; active patronage bonds involving a character at 16; active employment tenures per employee at 8; relationship consequences at 64 per character action; and witnesses at 32 per consequence. Completed proposals, memberships, bonds, and tenures retain 64 detailed records per category and involved character; recommendations retain 64 per involved character. Deterministic eviction folds counts and date range into checked fixed-size `CareerHistoryAggregate` records. A character may join multiple retinues because retinue membership is distinct from employment and faction allegiance; no universal loyalty or exclusive-service rule is invented here.

The local 1,000-character fixture submits and resolves 1,000 registered actions, producing 450 offers, 450 acceptances, 100 recommendations, 150 active memberships, 150 patronage bonds, 150 employment tenures, and 1,000 generic relationship memories. It records raw local Apple Silicon timings without brittle assertions. On the final corrected tree, the observed combined submit-and-resolve time was 15,649.077 ms, career query 2.200 ms, snapshot/checksum 80.037 ms, save 285.490 ms, and load 174.532 ms; the save was 387,824 bytes with checksum `313d5183aa8b3d60a064076da5a91082217771753c08f958212ef6880a45e55b`. This package measurement does not satisfy or waive the full SP-04 three-second turn budget, which remains unchecked and requires later profiling/optimization evidence.

Local integrated verification on 2026-07-15 passed `./scripts/validate.sh` and `./scripts/test.sh Release`: 243 Simulation.Core, 71 Game.Content, 6 Game.Application, and 18 repository tests. Focused runs passed 22 career tests and 99 save/recovery tests. Validation retained 1,295 records, 2,820 translations, and registry checksum `b04754a678bbb971045e4b2d602df5bf5c48fe26fc606b595449391e54d6b2a0`; the schema-6 frozen checksum is `90c27f2dd9954e0d3d2a304e9b661bf1ef25f2e4f620743d78bc60382d780bd4`, and the current ten-year/1,000-entity soak checksum is `14136a6a415a7c687cc5265709f1bbe7384c67527d9dc328dc111c4d6a523ff2`. `git diff --check`, `git lfs fsck`, and formatter verification over every C1-touched C# file passed. The whole-solution formatter additionally reports six pre-existing whitespace findings in unchanged `tools/Tools.ContentPipeline/LaterHanLocationImporter.cs`; C1 does not alter or claim to remediate that baseline.

Independent read-only review found five defects in the pre-closeout candidate: a public mutation bypass, stale consequences on invalidation, an unbounded consequence list, a missing recommendation role in affected IDs, and one authentication-order wording error. All five were corrected with focused regressions; targeted re-review found no unresolved issue in the fix delta. This remains local working-tree evidence, not clean-checkout, hosted, or cross-platform evidence.

Accepted revision `d5d2705d3516c67a06e127dcfa867a854b37a21f` subsequently passed hosted macOS arm64 and Windows x64 validation, the complete 243/71/6/18 suites, import, native export, automated smoke, manifest inspection, artifact upload, and static artifact verification. The [exact-SHA evidence report](evidence/SP-04C1-EXACT-SHA-d5d2705.md) preserves the run, jobs, checksums, and limitations. It does not relabel the local wall-clock measurement as hosted or satisfy the full SP-04 three-second budget.

Personal resources, faction/subfaction/imperial allegiance, offices/titles, marriage/romance, household expansion, birth/death resolution, inheritance/succession, content, UI, battle integration, and AI remain outside C1.

## SP-04C2 personal-wealth kernel

SP-04C2 adds `CharacterResourceWorldState` as a deterministic, checksum-covered, default-empty subsystem registered as `simulation.character_resources@1`. It models one sparse abstract-wealth account per character: an absent account means zero wealth, and zero-balance rows are not retained. This is a gameplay quantity, not a named historical currency, commodity, exchange-rate, income, estate-value, or economy model. The authoritative query returns defensive canonical accounts, recent ledger entries, and folded history.

`character_resource_action.v1` is the registered outer command envelope, with `transfer_wealth.v1` as its only C2 action. `character_resource_action_resolved.v1` is the persistent mutation event. Source and recipient must be distinct existing characters who have been born and are alive on the resolution date; the acting character must also be capable of acting. The amount must be positive. A valid transfer applies the source debit and recipient credit atomically and verifies conservation with checked wide arithmetic. If intervening commands leave insufficient source wealth or would overflow the recipient, resolution records an explicit cancellation and changes no account or ledger state. Commands scheduled for later days advance the resource subsystem calendar to their authoritative resolution date and turn.

Account, action-event, transfer, and ledger-entry IDs use versioned length-framed SHA-256 derivation. A successful transfer creates one outgoing and one incoming ledger entry with the exact command, event, date, turn, counterparty, and amount. At most 64 detailed entries are retained per involved character; deterministic eviction folds incoming/outgoing counts, amounts, and the date range into a checked fixed-size aggregate. Resolved events are re-planned and compared before mutation, and candidate state commits only after the complete outcome validates.

Save schema 8 adds the complete resource snapshot and system registration. Its authenticated 7→8 migration starts from the literal nonempty schema-7 fixture produced by accepted SP-04C1 revision `d5d2705d3516c67a06e127dcfa867a854b37a21f`, rejects injected future resource data, adds empty resource state, recomputes the destination checksum, and leaves the source file byte-identical. Canonical save/load, restore, checksum, registered diagnostic, current raw-shape, recovery, and full 1→8 migration coverage include the new subsystem.

The final local 1,000-character fixture planned and applied 1,000 direct transfers, producing 1,000 accounts and 2,000 retained ledger entries. On Apple Silicon macOS it measured 8,077.354 ms for transfer processing, 9.857 ms for query/snapshot serialization, 91.771 ms for the full-world checksum, 196.321 ms to save, and 95.900 ms to load. The save was 259,434 bytes and the snapshot JSON contained 1,462,065 characters; checksum `989c242311a374c57ea4c306219d08aac12f2b0111f44d1d4df659c1cc657bf0`. These are raw local measurements with correctness assertions but no wall-clock threshold. They do not meet or waive the still-unproven full SP-04 three-second turn budget.

Independent architecture and verification review found and remediated two integration gaps: later-day resource events initially retained the turn-start calendar, and stale recipient overflow lacked a campaign-path regression. The corrected tree advances the resource calendar and covers both insufficient-funds and overflow cancellation through registered campaign commands; final re-review found no remaining correctness or architectural issue. Local repository gates passed with 280 Simulation.Core, 71 Game.Content, 6 Game.Application, and 18 repository tests, a zero-warning Release build, unchanged content validation, touched-file formatter verification, `git diff --check`, and `git lfs fsck`. Those results established only the local working tree at that point.

Accepted revision `e2d9590afc409da30aef86226a8d90a0023fbda3` subsequently passed hosted macOS arm64 and Windows x64 validation, two complete 280/71/6/18 suite executions per platform, import, native export, automated smoke, manifest inspection, artifact upload, and static artifact verification. The [exact-SHA evidence report](evidence/SP-04C2-EXACT-SHA-e2d9590.md) preserves the run, jobs, checksums, and limitations. It does not relabel the local wall-clock measurement as hosted or satisfy the full SP-04 three-second budget.

Opaque estate holdings are reserved for SP-04C3. Household/family/faction/court treasuries, debt, currencies, commodities, income, spending, taxation, geography, land grants, prices, marriage/romance, lifecycle, inheritance/succession, content, UI, battle, and AI remain outside C2.

## SP-04C3 opaque character-estate holdings

SP-04C3 adds `CharacterEstateHoldingWorldState` as a deterministic, checksum-covered, default-empty subsystem registered as `simulation.character_estate_holdings@1`. Each version-1 record contains only an owner-independent `estate:` identity and one owning character. The authoritative query returns canonical defensive global and per-owner collections. Persisted owners must exist and be born by the snapshot date; dead, incapacitated, captive, or hostage owners remain valid so death cannot delete or silently reassign property before SP-04F resolves succession. A character may retain at most 64 holdings. SP-04F must handle a succession candidate that would exceed that accepted bound explicitly and atomically.

C3 is deliberately state-only. It registers no command, event, public mutator, transfer history, automatic dead-owner cleanup, or inheritance rule. `EstateId` remains stable when a separately constructed candidate changes its owner. SP-04F may later use that seam for inheritance, while SP-06 remains the owner of physical estate records, geography, acreage, control, grants, value, production, yield, rent, taxation, and administration. Household/family/faction/court ownership, economy, content, UI, battle, and AI are also outside C3.

Save schema 9 requires complete estate-holding state and `simulation.character_estate_holdings@1`. The literal 319,764-byte nonempty schema-8 fixture was generated from the exact accepted SP-04C2 source/API at `e2d9590afc409da30aef86226a8d90a0023fbda3`; its authoritative stored checksum is `ba485b0efc67e7cff38cf6de4b4536dbda2191ee87f5577ff1ee2d1d0031424f`, and the checked-in file SHA-256 is `6419cd00b10697fe04b2f1e32bca8cfdc59b3cfb02f50091113e8735afa55428`. It contains nonempty character, relationship, career, wealth-account, ledger, folded-history, and 65-command/event diagnostic state. The authenticated 8→9 migration rejects injected future estate data, adds only empty estate state and its system version, recomputes the destination checksum, and leaves source bytes unchanged.

The local Apple Silicon fixture constructs 1,000 characters and 8,000 holdings. Its final focused run measured 68.607 ms construction, 751.881 ms for 1,000 owner queries plus snapshot JSON serialization, 21.056 ms checksum, 325.787 ms save, and 210.018 ms load. The compressed save was 37,677 bytes, the estate snapshot JSON contained 1,016,034 characters, and checksum was `c033184371cf80b022a174acc5fa799345fa5eca9a89feec90f75b8a4c5bc83a`. These are raw local measurements with correctness assertions and no brittle threshold; they do not satisfy or waive the full SP-04 three-second turn budget.

Independent architecture review approved the separate estate subsystem without an ADR and confirmed that it does not import SP-06 meaning. Adversarial verification found a query-time eligibility coupling and weak checksum/migration assertions; the final tree separates persisted-owner birth validation from read-only query existence, covers shuffled and changed-owner/identity checksums, rejects semantic current-save corruption without changing source bytes, and compares the complete pre-C3 snapshot during migration. Final re-review found no open code finding. Repository-wide local gates passed with 312 Simulation.Core, 71 Game.Content, 6 Game.Application, and 18 repository tests, a zero-warning Release build, unchanged content validation, touched-file formatting, `git diff --check`, and `git lfs fsck`.

Accepted revision `7b9f795320e5f4c14aa7e14185e7ba035fdf6847` subsequently passed hosted macOS arm64 and Windows x64 validation, two complete 312/71/6/18 suite executions per platform, import, native export, automated smoke, manifest inspection, artifact upload, and static artifact verification. The [exact-SHA evidence report](evidence/SP-04C3-EXACT-SHA-7b9f795.md) preserves the run, jobs, checksums, and limitations. Physical Windows remains an M4 gate; signing/Steam remain SP-15 gates; the full SP-04 three-second budget remains unmet.

## SP-04D0 immutable marriage foundation

SP-04D0 adds `CharacterMarriageWorldState` as a deterministic, checksum-covered, default-empty subsystem registered as `simulation.character_marriages@1`. It stores explicit marriage practices, proposals, political betrothals, legal unions, adult non-explicit romance-route progress, and bounded folded history. Practices configure minimum legal-union and romance ages, principal-spouse and concubinage limits, childhood political-betrothal policy, widow-remarriage policy, and direct-line/sibling prohibitions. They are referenced explicitly and are never inferred from culture, family, or household membership.

Legal unions and romance routes require both participants to be adults under a practice whose configured minimum is never below 18. A minor may enter only a political betrothal when its explicit practice permits that state; it cannot enter a legal union or romance route. Voluntary eligibility applies life, incapacity, and custody checks. Coercive state remains classified as political and can never be classified as romantic; D0 records no positive relationship effect or scene. Active established unions survive later incapacity or custody until a later package resolves an explicit end.

Persisted state uses canonical pairs, namespaced IDs, authoritative calendar bounds, exact practice and participant references, exact proposal/outcome causality, and unique creation-command identities. Every accepted proposal owns exactly one correctly typed betrothal or union. A fulfilled betrothal identifies one exact political-arrangement union and shares its authoritative fulfillment command/date/turn. Nonaccepted proposals own no outcome, and a creation command cannot simultaneously establish a proposal and a positive romance route. Folded history cannot predate its character. Eligibility applies the same duplicate, global 64-active-legal-relationship, and configured form limits that construction enforces.

Detailed state is bounded to 8 active proposals per recipient and 64 retained proposals, betrothals, unions, and romance routes per involved character and category. Authoritative global and per-character queries are canonical defensive copies. `CharacterWorldSnapshot.HouseholdStates` remains the sole household residence/membership authority: D0 introduces no duplicate household field, guardianship inference, authority inference, household movement, or decision behavior.

D0 is deliberately immutable. It registers no marriage command, event, public mutator, runtime progression, relationship-memory consequence, household action, faction/court/diplomacy integration, lifecycle behavior, succession behavior, content, localization, scene, UI, AI, or platform dependency. SP-04D1 owns political proposal/union workflow, D2 owns adult non-explicit romance progression, and D3 owns household decisions, conflict, and coercion effects.

Save schema 10 requires the complete marriage snapshot and exact system registration. The 320,019-byte literal schema-9 fixture has stored historical checksum `1ef0f8728311ab217e84d9e6ff432342a7bac85b74aae6eee2cf92159d541684` and file SHA-256 `ab0df6f7740af51bc4eed7d73d97fd5dba38ea273db738c511a289e6bea084ce`. It retains two characters, nonempty relationship/career/resource state, one estate holding (`estate:fixture/leader_manor`), folded history, and 65 command/event diagnostics. A detached exact-revision test proved that accepted C3 commit `7b9f795320e5f4c14aa7e14185e7ba035fdf6847` computes the same schema-9 checksum. The authenticated 9→10 migration rejects injected future marriage data, adds only empty marriage state/system registration, recomputes the current checksum, and preserves the complete source and source bytes. Current raw-shape, semantic-corruption, recovery, restore, checksum, and round-trip coverage includes D0.

Local integrated verification on 2026-07-15 used Darwin arm64 and .NET SDK 10.0.301. `./scripts/validate.sh` retained 1,295 records and 2,820 translations with registry checksum `b04754a678bbb971045e4b2d602df5bf5c48fe26fc606b595449391e54d6b2a0`. `./scripts/test.sh Release` built with zero warnings and passed 374 Simulation.Core, 71 Game.Content, 6 Game.Application, and 18 repository tests. The focused marriage/save slice passed 200 tests. Touched-file formatting, `git diff --check`, and `git lfs fsck` passed. The ten-year/1,000-entity soak checksum is `0a92aa6dec435a9b33a399898ed7985210d7142dc10027b23a0bc4e392666b36`.

The local Apple Silicon fixture constructs 1,000 characters with 250 proposals and 250 romance routes, then performs 1,000 aggregate record queries. Its final Release run measured 38.405 ms construction, 67.561 ms queries, and 191.298 ms for snapshot plus full-world checksum; checksum `6542e48b7ccdceee890fc757fd9104c6b4f73164ef4263b7b451947766777e06`. These are raw local correctness measurements without a wall-clock threshold. They do not satisfy or waive the full SP-04 three-second campaign-turn budget.

Independent adversarial review remediated exact-causality, future-turn, eligibility-parity, pre-birth-history, save-evidence, coercive-age, and coercion-versus-positive-romance gaps. The final rule blocks coercive commands from creating or completing positive romance state while explicitly allowing the same command to end or invalidate an existing route. Release-focused re-review passed 38/38 marriage tests and found no remaining D0 correctness or package-boundary blocker.

Accepted revision `f7fef247178776d7c6fb1c4bed56f09dece76ff4` subsequently passed hosted macOS arm64 and Windows x64 validation, two complete 374/71/6/18 suite executions per platform, import, native export, automated smoke, manifest inspection, artifact upload, and static artifact verification. The [exact-SHA evidence report](evidence/SP-04D0-EXACT-SHA-f7fef24.md) preserves the run, jobs, checksums, and limitations. Physical Windows remains an M4 gate; signing/Steam remain SP-15 gates; the full SP-04 three-second budget remains unmet.

## SP-04D1 political marriage workflow

SP-04D1 adds registered `character_marriage_action.v1` commands and `character_marriage_action_resolved.v1` events without changing the version-1 marriage snapshot/query/system contracts. Five participant actions cover political proposal creation, recipient accept/refuse, proposer withdrawal, either-participant betrothal cancellation, and either-participant adult fulfillment. Every created proposal is forced to `Political` plus `PoliticalArrangement`; D1 cannot create romantic or coerced state, progress a romance route, move a household member, infer third-party authority, or create relationship effects.

Direct legal proposals require both participants to meet the exact practice age, life, capacity, custody, kinship, widow-remarriage, form, duplicate, and global-limit rules. Political betrothal may include a minor only when the selected practice explicitly allows it. Acceptance revalidates authoritative state: a valid legal acceptance atomically creates one union; a valid betrothal acceptance atomically creates one betrothal; and an intervening eligibility failure terminalizes the still-active proposal as `Cancelled` without partial state. Missing or already-terminal targets and temporarily ineligible fulfillment produce generic command cancellation. A successful fulfillment creates a second legal proposal already accepted by the fulfillment command, creates its political-arrangement union, fulfills the original betrothal, and shares the same command/date/turn across the chain.

Action events are replanned from authoritative state, compared as complete typed JSON, built into a fully validated candidate, and only then committed. Apply-time code requires the Commands phase, exact causal/event IDs, canonical affected IDs, authoritative date/turn, and exact nested outcome. Same-turn duplicate, recipient-limit, mixed-practice, and retention-capacity races therefore resolve deterministically as typed or generic cancellation rather than aborting the turn. The marriage calendar advances monotonically with the world calendar, and restored pending D1 commands replay to the same events and checksum.

Retention uses proposal-root causal groups. An accepted proposal and its direct betrothal/union remain together; fulfillment links both proposal roots into one four-record proposal/betrothal/proposal/union group. Active outcomes pin the complete group, the two proposal roots consume two proposal slots for both participants, and a group is retained or folded atomically by its latest child terminal turn/date and stable group ID. Saturated fulfillment cancels before mutation. Folded counts and date ranges are checked and written for both participants. This causal grouping may leave an unused proposal slot when the next terminal group costs two slots; it never splits a retained source/outcome chain.

Save schema 11 is required because schema-10 readers do not know the new command/event discriminators. The authenticated 10→11 migration validates the exact D0 historical checksum and complete marriage shape, rejects D1 discriminators from schema-10 pending commands and diagnostic commands/events, preserves the snapshot and diagnostics, and changes only the envelope schema/checksum contract. The frozen exact-D0 fixture identity is recorded above. Current schema-11 save/load, recovery, semantic validation, pending-command replay, unknown nested discriminators, and the full 1→11 forward chain are covered.

Final local closeout uses Darwin arm64, .NET SDK 10.0.301, and Godot 4.6.1. The focused marriage/save slice passes 233 tests; the complete repository gate passes 407 Simulation.Core, 71 Game.Content, 6 Game.Application, and 18 repository tests with zero warnings. Validation retains 1,295 records and 2,820 translations at registry checksum `b04754a678bbb971045e4b2d602df5bf5c48fe26fc606b595449391e54d6b2a0`; the ten-year golden remains `0a92aa6dec435a9b33a399898ed7985210d7142dc10027b23a0bc4e392666b36`. Touched-file formatting, diff, and LFS checks pass. The representative local 1,000-character/250-proposal run measured 1,525.199 ms for workflow and 120.596 ms for snapshot/checksum, produced 930,668 bytes JSON and 32,440 bytes gzip, and checksum `9610a37a8e68825fa9d907ab58832240f1357b5f3ef227f4d61a57bd86f8d215`. These are raw local component measurements without a wall-clock assertion; they do not establish the full SP-04 three-second campaign-turn budget.

Accepted revision `653ce71d24bd81435ded9e65022dc29afd8f4810` subsequently passed hosted macOS arm64 and Windows x64 validation, two complete 407/71/6/18 suite executions per platform, import, native export, automated smoke, manifest inspection, artifact upload, and authenticated static artifact verification. The [exact-SHA evidence report](evidence/SP-04D1-EXACT-SHA-653ce71.md) preserves the run, jobs, checksums, and limitations. Physical Windows, signing/Steam, and the full SP-04 three-second budget remain open gates.

## SP-04D2 mutual-consent romance workflow

SP-04D2 adds five nested version-1 actions under the unchanged `character_marriage_action.v1` outer payload: offer a route, accept/refuse an invitation, withdraw an invitation, advance a route by one expected level, or end a route. Only the recipient may accept/refuse, only the initiator may withdraw, and either participant may advance or end. Offer and acceptance provide an explicit bilateral handshake; acceptance atomically removes the active invitation and creates one version-2 route at progress 1. A route advances exactly one step, completes atomically at level 4, and may be ended by either participant. It never creates a proposal, betrothal, union, relationship or memory effect, household mutation, scene, content, UI, AI, or platform behavior.

Positive actions revalidate exact adult/practice, life, capacity, custody, and kinship eligibility. `ExpectedProgressLevel` prevents queued commands from skipping levels. Stable event order resolves same-pair offers, response conflicts, repeated advances, advance/end conflicts, and completion/end conflicts; stale or losing commands cancel without partial mutation. Stale acceptance eligibility produces a typed invitation cancellation and removes the invitation, while route-capacity failure cancels generically and preserves it for retry. D2 does not create `Invalidated`; lifecycle/condition integration remains reserved for D3. Ending does not require the other participant's agreement. Political betrothal or union state may coexist with and diverge from romance state.

Version-2 routes embed the source invitation ID, initiator, invitation command/date/turn, acceptance command, and last-positive-progress command/date/turn. Length-framed SHA-256 derives invitations from creation date plus offer command and routes from invitation plus acceptance command. Progress-1 evidence must equal acceptance; later progress must identify a distinct, uniquely retained advance command. Legacy version-1 routes remain version 1 and actionable at levels 0 through 4 without synthesized evidence. Invitations are active-only, unique per pair, bounded to 8 incoming and 64 involving each character. Existing route retention remains 64 per participant: active routes pin, terminal routes age by resolution turn/date/ID, and folding updates both participants atomically.

Save schema 12 and `simulation.character_marriages@2` are required for invitations, route-v2 evidence, and D2 nested discriminators. The exact-D1 schema-11 fixture and authenticated 11→12 behavior are recorded in the persistence section above. Current saves cover every pending D2 action, every nested action/outcome discriminator, semantic causal tamper, unknown data, recovery, replay, checksum, and legacy migration. Duplicate historical marriage-system registrations fail as controlled compatibility errors so autosave recovery can continue.

Final local verification on 2026-07-15 uses Darwin arm64, .NET SDK 10.0.301, and Godot 4.6.1. `./scripts/validate.sh` retains 1,295 records and 2,820 translations at registry checksum `b04754a678bbb971045e4b2d602df5bf5c48fe26fc606b595449391e54d6b2a0`. `./scripts/test.sh Release` builds with zero warnings and passes 469 Simulation.Core, 71 Game.Content, 6 Game.Application, and 18 repository tests. The focused marriage/save/ten-year-soak slice passes 296 tests. The ten-year/1,000-entity golden is `ba4eccd512e7bf699c3360032f2a5f007b362cc16ff718a487a6d082357e65b2`. The current raw 1,000-character/200-completed-route run measured 7,238.257 ms for workflow and 113.094 ms for snapshot/checksum, produced 1,009,019 bytes JSON and 48,844 bytes gzip, and checksum `6035ff878b94f261a676af83bb2d293b3585e9eb40f650721399c39f96539af0`. These are local working-tree measurements without a wall-clock assertion and do not establish the full SP-04 three-second campaign-turn budget.

Accepted revision `62a50075ca86b3466cca9c05825d4374e6cac366` subsequently passed hosted macOS arm64 and Windows x64 validation, two complete 469/71/6/18 suite executions per platform, import, native export, automated smoke, manifest inspection, artifact upload, and authenticated static artifact verification. The [exact-SHA evidence report](evidence/SP-04D2-EXACT-SHA-62a5007.md) preserves the run, jobs, checksums, and limitations. Full SP-04 acceptance, the three-second campaign-turn budget, physical Windows, signing, Steam, and later D3/E/F/G/X packages remained open at D2 acceptance.

## SP-04D3 household decisions and lifecycle integration

SP-04D3 registers `character_condition_action.v1` for reserved-system incapacity, recovery, custody-entry, and custody-release changes, plus `household_decision.v1` for head-authorized expulsion and exact-custodian captive/hostage incorporation. Condition actions carry the exact expected current state and revalidate every transition. Household actions cannot expel or move a head, create/dissolve households, infer authority from family identity, or change custody as a side effect. Successful apply prepares and validates every affected subsystem before atomically replacing the existing character-state contents in place.

`impose_coerced_union.v1` extends the unchanged marriage outer command. A living, capable, free adult exact custodian may create one accepted political/coerced proposal and active political/coerced union with a living legal-age captive or hostage. The action never provides romantic consent, moves a household, or creates positive romance. It invalidates an active route for the exact pair and creates a harmful target-to-custodian memory. Custody entry and household coercion likewise create exact target-to-actor memories. Attraction is always zero; the bounded deterministic saturation ladder raises resentment, fear, and rivalry before reducing trust, affection, respect, and compatibility.

Incapacity/custody invalidates consent-dependent active proposals, removes invitations, and invalidates active routes without ending accepted political betrothals or established coerced unions. Recovery/release never resurrects invalidated state. An internal non-mutating death preview proves the later public-death aggregate can invalidate all active proposals/betrothals/invitations/routes and end active unions with `SpouseDied`, but D3 exposes no public death command and performs no succession, inheritance, or ownership transfer.

Final local verification passes the zero-warning Release build, 490 Simulation.Core, 71 Game.Content, 6 Game.Application, and 18 repository tests, unchanged content validation, touched-file formatting, diff, and LFS gates. The local 1,000-character fixture records 200 coerced unions, 200 releases, and 200 incapacity changes without a wall-clock assertion; it does not satisfy the full three-second turn budget. Accepted revision `93d38810a87707a7c4c98c7392e2a2f20dc030fb` passed hosted macOS arm64 and Windows x64 validation, two complete suite executions per platform, import, native export/smoke, manifest inspection, artifact upload, and authenticated static artifact verification. The [exact-SHA evidence report](evidence/SP-04D3-EXACT-SHA-93d3881.md) preserves the run, artifacts, checksums, and limitations. Full SP-04 acceptance, birth/public-death/succession, physical Windows, signing, Steam, and later E/F/G/X packages remain open.

## SP-04E0 legal-adoptive parent establishment

`character_family_action.v1` is a reserved-system Commands-phase envelope. Its sole E0 action names an adoptive parent, an adoptee, and the exact canonical current parent-link list. The adopter must be alive, free, capable, at least 18 on the exact resolution date, and born before the adoptee. The adoptee must be alive and free; an adult adoptee must be capable. A successful event appends exactly one `LegalAdoptive` link and derives the reciprocal child link without changing biological/legacy links, descriptors, condition, family, household, relationships, marriage records, or any other subsystem.

New actions allow at most two legal-adoptive parents, four total parents, and 64 legal-adoptive children per adopter. These are action-local bounds: old valid v2 snapshots remain accepted. Self-parentage, duplicate pairs, cycles, stale/noncanonical expected links, and invalid life/capacity/custody state fail before mutation. The candidate parent graph must also validate the complete retained marriage snapshot, including terminal proposal, betrothal, union, invitation, and route records, against each practice's direct-line and sibling restrictions. An incompatible adoption is rejected atomically; E0 never rewrites marriage history.

The durable record is current typed parentage. E0 does not add adoption revocation, an effective date ledger, guardianship, household residence, consent, inheritance rights, or succession rules. Resolution source/date metadata exists in the bounded diagnostic event only. Once evicted, a link without separate history must be treated as history-unknown; later packages must not reconstruct an effective date.

Final local verification passes the zero-warning Release build, 539 Simulation.Core, 71 Game.Content, 6 Game.Application, and 18 repository tests, plus the focused 48-test family/schema-13 slice, unchanged content validation, touched-file formatting, diff, and LFS gates. Accepted revision `30fd0ad5f9a47eb15c0af27360ae31d72414a8ed` passed hosted macOS arm64 and Windows x64 validation, two complete suite executions per platform, import, native export/smoke, manifest inspection, artifact upload, and authenticated static artifact verification. The [exact-SHA evidence report](evidence/SP-04E0-EXACT-SHA-30fd0ad.md) preserves the run, artifacts, checksums, and limitations. Full SP-04 acceptance, later E/F/G/X packages, physical Windows, signing, and Steam remain open.

## SP-04E1 primary guardianship

`simulation.character_guardianships@1` stores bounded, source-backed primary-guardianship records separately from character parentage, family, household, custody, relationships, and marriage. `IAuthoritativeCharacterGuardianshipWorldQuery` returns defensive canonical copies of all records, the active record for a ward, or all records involving a character. At most one record may be active for one ward, and no character may be involved in more than 64 retained records.

`EstablishPrimaryGuardianshipAction` extends the existing reserved-system `character_family_action.v1` envelope. The exact guardian and ward must exist, be born and alive, and differ. The guardian must be older, at least 18 on the resolution date, capable, and free. The ward must be under 18; ward incapacity and custody are allowed. Biological parents, legal-adoptive parents, and unrelated adults use the same establishment rules. The action's expected active-guardianship ID prevents stale queued work, and exact event ordering deterministically chooses one winner when two guardians are proposed for the same ward.

The durable record keeps establishment date/turn and command/event identity. Active and ended shapes are versioned now so later lifecycle packages can end records without replacing the contract, but E1 itself creates active records only. Snapshot validation checks historical age and source evidence and deliberately does not require the participants' current minority, life, capacity, freedom, or custody state. No guardian authority over education, residence, custody, inheritance, succession, or adult regency is implied.

Schema 15 requires a complete version-1 guardianship snapshot and system registration. Its exact-E0 schema-14 fixture authenticates before migration, rejects E1 vocabulary/state injection, and migrates by adding an empty guardianship subsystem only. The accepted package covers authority, age and condition boundaries, biological/adoptive/unrelated guardians, one-per-ward and retention limits, immutable queries, subsystem isolation, tamper rollback, both race assignments, pending replay, current-save diagnostics, checksum behavior, exact-source migration, current/standalone compatibility, and a raw 1,000-character/64-guardianship fixture.

Final local verification on 2026-07-16 passes the zero-warning Release build and 580 Simulation.Core, 71 Game.Content, 6 Game.Application, and 18 repository tests, unchanged content validation, touched-file formatting, diff, and LFS gates. Independent review found and remediated stale guardianship-calendar state after eventless turns; the final read-only rerun reports no remaining blocker. Accepted revision `97b607ae8df77dbd5c6fa5ab6b544000208cdb0e` passed hosted macOS arm64 and Windows x64 validation, two complete suite executions per platform, import, native export/smoke, manifest inspection, artifact upload, and authenticated static artifact verification. The [exact-SHA evidence report](evidence/SP-04E1-EXACT-SHA-97b607a.md) preserves the run, artifacts, checksums, and limitations. Full SP-04 acceptance and later E/F/G/X packages remain open.

## SP-04E2 guardianship termination and replacement

`EndPrimaryGuardianshipAction` and `ReplacePrimaryGuardianshipAction` reuse the reserved-system family-action envelope and guardianship-v1 state. End requires the ward's exact active record and accepts only explicit revocation, or guardian-unavailable when the guardian is dead, incapacitated, or not free. Replacement requires a living minor ward and a different older adult guardian who is alive, capable, and free; it atomically ends the old record as `Replaced` and creates one active replacement from the same command/event.

Both workflows revalidate retained-record capacity, deterministic ID collision, exact affected IDs, and state again at apply time. Same-turn end/end, end/replace, and replace/replace races select the first hashed event and cancel stale competitors. No character, parentage, family, household, custody, relationship, marriage, education, claim, inheritance, or succession state changes. Automatic coming-of-age/death termination, character-issued authority/consent, co-guardians, residence movement, education authority, and adult regency remain deferred.

Schema 16 is vocabulary-only. The exact-E1 61,267-byte schema-15 fixture authenticates with checksum `ed398b879de95b9065bffb367fb3a7b830c40d6a8e1f29d3a0a64ffb414e5b86` and file SHA-256 `51a6ecf8e0556af35cbe4010645925d50bc965ca4a2d07ea05c8c9c486538fc3`; it preserves active and terminal E1 records while rejecting E2 action/outcome vocabulary and properties. Local gates pass 602 Simulation.Core, 71 Game.Content, 6 Game.Application, and 18 repository tests with zero warnings and unchanged content validation. Independent read-only review reports no remaining correctness or package-boundary blocker after exact-JSON evidence remediation. Accepted revision `7491da89985fedb18e423082a2fd9187b8899e52` passed hosted macOS arm64 and Windows x64 validation, two complete suite executions per platform, import, native export/smoke, manifest inspection, artifact upload, and authenticated static artifact verification. The [exact-SHA evidence report](evidence/SP-04E2-EXACT-SHA-7491da8.md) preserves the run, artifacts, checksums, and limitations. Full SP-04 acceptance and later E/F/G/X packages remain open.

## SP-04E3 deterministic coming of age

The reserved `system:simulation/character_lifecycle` actor generates one internal priority-zero Systems-phase `character_coming_of_age.v1` command when a living character's exact age changes from 17 to 18. The derived `character_came_of_age.v1` event is causally stable and observable even without an active guardian. An exact active primary guardianship ends atomically as `WardCameOfAge`. No adult flag or lifecycle history is added to character state, and no already-adult transition is backfilled. Exact date handling includes later dates within multi-day turns and March 1 for February 29 births in non-leap years.

Commands-phase guardianship work precedes lifecycle resolution. A valid same-day revocation or guardian-unavailable ending leaves no record for E3 to close; a valid replacement from an earlier day is closed on the birthday. External lifecycle command submission and pending lifecycle restoration fail closed. Apply-time replanning rejects phase, priority, causal/event identity, affected-ID, payload, and stale-state changes without mutation. Other character fields and all non-guardianship subsystems remain unchanged.

Schema 17 is vocabulary-only. The exact-E2 64,549-byte schema-16 fixture authenticates with stored checksum `aac74df0b0c97f3ec49c6e2aeaef1be742f102a797f7d317b576e1e8ed2da471` and file SHA-256 `3f118e24486fc7367dc00f07419abfb19eff13a144daffc878ee73a99277fbe5`; it preserves rich E2 state and diagnostics while rejecting E3 discriminators and properties, including explicit null. Local gates pass 625 Simulation.Core, 71 Game.Content, 6 Game.Application, and 18 repository tests with zero warnings and unchanged content validation. Independent review's birthday-replacement and schema-15/16 corruption evidence gaps are remediated. Accepted revision `59588be9d277dc4c4cb7ec98ef99e33591b0eeda` passed hosted macOS arm64 and Windows x64 validation, two complete suite executions per platform, import, native export/smoke, manifest inspection, artifact upload, and authenticated static artifact verification. The [exact-SHA evidence report](evidence/SP-04E3-EXACT-SHA-59588be.md) preserves the run, artifacts, checksums, and limitations. Full SP-04 acceptance and later E/F/G/X packages remain open.

## SP-04E4 adult union-linked pregnancy registration

`simulation.character_pregnancies@1` is an active-only subsystem. Its canonical snapshot stores the pregnancy ID, explicit gestational and other biological parent roles, exact source union, start and expected-birth dates, start turn, and source command/event IDs. Authoritative queries return defensive copies by gestational parent, source union, or involved character. At most one active pregnancy may exist for a gestational parent or source union.

`RegisterActivePregnancyAction` extends the reserved-system `character_family_action.v1` envelope. Both explicit parents must be distinct, known, born, living, and at least 18 on the resolution date. The exact retained source union must be active, connect that pair, and already have started. The expected-current pregnancy ID must match exactly; success therefore requires null in E4. A fixed version-1 gestation derives `ExpectedBirthDate` as the start date plus 280 days, and calendar overflow fails before mutation. Stable pregnancy identity includes the source event, both ordered roles, and union, so reversing the explicit gestational role changes the identity without inferring sex, gender, fertility, or consent.

Apply reconstructs the complete candidate and rejects stale parent/union/pregnancy state, replay, collision, date/turn, causal/event, affected-ID, or payload tampering without mutation. Schema 18 also fails closed when an active pregnancy is restored against an ended union or either currently dead parent; the first package that exposes either lifecycle transition must define its atomic pregnancy handling before relaxing that rule. Commands competing for the same gestational parent or union choose one winner by the campaign's canonical priority-then-event-ID order and cancel the loser; equal-priority races therefore choose the first derived event in either submission order. Independent registrations commute and produce the same checksum. Characters, parentage, family, household, condition, relationships, careers, resources, estates, marriage, guardianship, geography, and random streams remain unchanged. Advancing beyond the expected-birth date deliberately leaves the active record untouched: E4 adds no conception scheduler, pregnancy loss, birth, child identity or parent links, inheritance, education, death integration, content, localization, UI, or AI.

Save schema 18 adds the complete pregnancy-v1 snapshot and `simulation.character_pregnancies@1`. Its exact-E3 schema-17 fixture was generated by a detached harness compiled against accepted E3 production source `59588be9d277dc4c4cb7ec98ef99e33591b0eeda`. The 68,407-byte fixture retains rich prior state and diagnostics, one pending pre-E4 family action, and one E3 coming-of-age event with atomic `WardCameOfAge` guardianship closure. It has stored checksum `7a680781eceabf8c46554f780aaaca0ef6f781caf3256cac46185f0031e88ea4` and file SHA-256 `c99c1183f408dfd66eb08015e3b10d4e5b3a2f573c4adb364cd395fb8a1eb9c2`. Schema 17 rejects pregnancy state, system registration, nested action/outcome discriminators, and E4-only properties including explicit null before migration.

Local Release verification on 2026-07-16 passes `./scripts/validate.sh`, the zero-warning repository build, 674 Simulation.Core tests, 71 Game.Content tests, 6 Game.Application tests, and 18 repository tests. Validation retains 1,295 records and 2,820 translations at unchanged registry checksum `b04754a678bbb971045e4b2d602df5bf5c48fe26fc606b595449391e54d6b2a0`. The representative 1,000-character/one-union fixture measured 52.019 ms for registration workflow and 127.504 ms for checksum, serialized to 848,328 JSON bytes and 17,655 gzip bytes, and produced checksum `46e57978a12f0849515880c1996fe07ead12932135503856522aa7b1ac3cb5ed`. These are raw local measurements without a wall-clock assertion and do not establish the full SP-04 campaign-turn budget.

Accepted revision `177346b7358e84da358f3bfac8057b6ea70ed412` passed hosted macOS arm64 and Windows x64 validation, two complete suite executions per platform, import, native export/smoke, manifest inspection, artifact upload, and authenticated static artifact verification. The [exact-SHA evidence report](evidence/SP-04E4-EXACT-SHA-177346b.md) preserves the run, artifacts, checksums, and limitations. Full SP-04 acceptance and later birth/education/death/inheritance/succession packages remain open.

## SP-04E5 deterministic pregnancy birth resolution

`ResolvePregnancyBirthAction` extends the reserved-system `character_family_action.v1` envelope without adding a scheduler. It names the exact active pregnancy and a versioned generated-newborn specification. Resolution is allowed on or after the pregnancy's expected birth date and revalidates both living parents, the exact active source union, current parent family/household/culture values, a canonical distinct parental trait subset of at most eight, and collision-free stable child/birth IDs. The primary name must be a `loc:` key; no raw text or live name generation enters Core.

The pregnancy-removal and character-insertion candidates are prepared before either is applied. The child ID derives only from the pregnancy; the birth record derives from the successful family event plus pregnancy. The child keeps `ExpectedBirthDate` even when resolution is overdue, while `ResolutionDate` records the later action. Its exact definition has `Generated/Fictional` provenance with the birth record as `RecordId`, no pack/override/source lineage, null courtesy name and origin location, empty abilities/aptitudes/ambitions/reputations/flaws, selected parental traits and culture, and default living condition. Its state contains exactly two canonical biological parent links. Selected family and household membership append canonically to existing state; no family/household definition or head is created.

Apply replans the exact action, outcome, child definition/state, causal/event identity, affected IDs, pregnancy removal, and character candidate. Same-pregnancy races choose one priority/event-ID winner and cancel stale work; independent births commute under shared family/household insertion. Earlier same-day household decisions may invalidate a queued placement and cancel the birth without removing the pregnancy. Replay, early/stale state, dead parent, ended union, invalid placement/trait/name, collision, payload/identity/affected-ID tampering, and reconstruction failure leave both subsystems and every random stream unchanged.

Schema 19 adds only E5 family-action vocabulary. Its frozen 74,121-byte schema-18 fixture was generated from exact accepted E4 implementation source `177346b7358e84da358f3bfac8057b6ea70ed412`; it retains one active pregnancy, one pending and one resolved E4 family action, and rich E0–E3 history. The stored checksum is `7f0feb49415b8d0074d447381340aea0c09200964f55104034db60c04f70d49e`, and file SHA-256 is `b27ccfdc51704721055161d6e57738e40030ff67d96f928e61bcbf5ed93c9453`. Schema 18 authenticates before migration, rejects E5 discriminators/properties including explicit null, preserves source bytes, and retains compatibility with generic pre-E5 generated children. A migrated overdue pregnancy resolves with its original expected birth date.

E5 changes no authored content or localization catalog. Authored character-v2 records continue to reject `Generated` origin; deterministic runtime generation is not a shipped asset. The `loc:` namespace prevents raw player-facing text but does not prove Korean/English resolution because Core has no content registry. A later presentation/name package must supply bilingual dynamic-name evidence. Final local gates build with zero warnings and pass 712 Simulation.Core, 71 Game.Content, 6 Game.Application, and 18 repository tests; unchanged content validation, diff, LFS, a 39-test independent focused slice, and independent full-Core re-run pass with no remaining blocker. Accepted revision `4b28fb74bed9181ce021e1c5e32ef9d039b4e2e1` passed hosted macOS arm64 and Windows x64 validation, two complete suite executions per platform, import, native export/smoke, manifest inspection, artifact upload, and authenticated static artifact verification. The [exact-SHA evidence report](evidence/SP-04E5-EXACT-SHA-4b28fb7.md) preserves the run, artifacts, checksums, and limitations.

## SP-04E6 primary-guardian education attainment

`CompletePrimaryGuardianEducationAction` extends the reserved-system, Commands-phase character-family envelope. It carries the ward, exact active primary-guardianship ID, and target ability. The guardian is the teacher and must be a living, capable, free adult with that ability in the immutable definition baseline. The living, capable ward must be born, under 18, and lack the ability; ward custody is deliberately allowed and no minimum age is inferred. Acquired abilities never qualify a teacher. On success, Core derives a stable attainment ID and appends one source-backed record containing the ward, teacher, guardianship, ability, resolution date/turn, and causal command/event.

Each ward may retain at most 64 attainments, with one record per ability and no eviction. Effective profile abilities are baseline union attained abilities. Same-ability races select the first priority/event-ID action and cancel stale competitors; different abilities commute. Earlier guardianship end/replacement or ward-incapacity work invalidates queued education, while education that resolves first remains valid retained evidence after the same-day guardianship or condition transition. An action on the exact eighteenth birthday fails before the Systems-phase coming-of-age event.

Schema 20 advances runtime character snapshot/state/profile/query/system contracts to v3 while definitions stay v2. Its 17,797-byte exact-E5 schema-19 fixture has stored checksum `dc49cef5fe5d55a310ec0191189358a188224afd8df33fc689efc4c34edd999c` and file SHA-256 `d6af4142e95b38fbd47386668de7b85253f345f29699258c0b9dce99b87369f0`. A detached harness compiled from E5 implementation `4b28fb74bed9181ce021e1c5e32ef9d039b4e2e1` generated it with a generated child plus pending/resolved E5 birth vocabulary. Migration inserts empty attainment arrays, upgrades retained diagnostic child state, preserves earlier data/source bytes, and lets that child subsequently gain the fixture ability through public guardianship and education workflows. Schema 19 rejects every E6 discriminator, isolated property including explicit null, and character-v3 version injection.

Authored v1/v2 content remains unchanged and normalizes to runtime v3 with empty attainments. No published schema, shipped content, localization, manifest, or provenance data changes. E6 adds no education progress/scheduler/curriculum/levels, RNG, mutable traits/aptitudes, relationship effects, public death, inheritance, succession, content, UI, or AI. Local repository gates pass 758 Simulation.Core, 73 Game.Content, 6 Game.Application, and 18 repository tests with zero build warnings. Accepted revision `0928e4484ef41da5ac31f5683af5347800a92dec` passed hosted macOS arm64 and Windows x64 validation, two complete suite executions per platform, import, native export/smoke, manifest inspection, artifact upload, and authenticated static artifact verification. The [exact-SHA evidence report](evidence/SP-04E6-EXACT-SHA-0928e44.md) preserves the run, artifacts, checksums, and limitations.

## SP-04F0 restricted public-death lifecycle foundation

`ResolveCharacterDeathAction` extends the existing reserved-system character-condition envelope. It requires a born living target and exact expected current condition. Success produces one stable `character_death:` record and the canonical dead condition, then atomically applies existing marriage death closure, ends active primary guardianships as `WardDied` or `GuardianDied`, and removes active pregnancies involving either explicit parent role. The event retains all four canonical change sets and is reconstructed completely before any subsystem mutates.

F0 fails closed for a household head, a current custodian of another character, any retained retinue leader, active career proposal role/direct-character principal, or active retinue/patronage/employment role. It does not invent replacement heads, captive disposition, career death reasons, or retinue transfer. Dead-owned wealth and estates, relationships/memories, parentage, education, family membership, non-head household membership, recommendations, and completed history remain unchanged as later inheritance/succession inputs.

Schema 21 adds only `resolve_character_death.v1`, `character_death_resolved.v1`, and their nested outcome properties. All subsystem snapshot/system versions remain unchanged. The exact-E6 schema-20 fixture is 16,755 bytes with stored checksum `9ad3e044f7491d4eca1f9e6afa5916ff2d3039efd687cfc944b1f10c82b6ef51` and file SHA-256 `94830ad5b8a6d1862a2cca7e838f69148adeafa591a9709862169959e148ca1e`; migration is vocabulary-only, source-byte preserving, and rejects isolated future vocabulary including explicit null. Authored content, localization, provenance, manifests, and `Game.Content` remain unchanged.

The 39-test focused slice covers both pregnancy roles; authority, phase, eligibility, and head/custodian/career/retinue blockers; day-two/three/four pending replay; condition, registration, due-birth, guardianship, education, marriage, career, and simultaneous-death races; tamper rollback; pending/resolved persistence; exact-fixture authentication; and post-migration continuation. The raw 1,000-character/200-death fixture measured 3,260.128 ms workflow, 0.426 ms query, and 105.547 ms checksum on local Apple Silicon, with 851,856 JSON bytes, 17,161 gzip bytes, and checksum `7596072b833a0a56dae1771ccd7d6c437c23c11937351f1566ff4c8418c13f0f`. This is not the full SP-04 performance gate. Accepted revision `783ccfb61357248158cf287ee69ba27b56c38f4a` passed [exact-SHA hosted macOS arm64/Windows x64 validation, complete suites, native export/smoke, and artifact inspection](evidence/SP-04F0-EXACT-SHA-783ccfb.md). Inheritance, heir designation, claims, regency, offices/titles, disputed succession, player transfer, career death closure, content, UI, AI, and battle remain pending.

## SP-04F1 career-death obligation closure

F1 advances only the nested death-change contract to v2 and adds a v1 career-death change set. Public death now invalidates every active proposal involving the target as proposer, recipient, or direct-character principal and ends target-involving memberships, patronage, and employment with six exact role-specific death reasons. All subsystem candidates validate before mutation; one career retention pass preserves complete event evidence and fails atomically on history overflow. Career snapshot/state/action/outcome/query/system contracts remain v1.

Retained retinues remain unchanged historical identities: leader death ends every active membership but creates no replacement leader, successor, transfer, or membership. Completed career records and recommendations remain unchanged except deterministic bounded folding. Household-head and current-custodian blockers remain. Household principals are not expanded to every household member.

Schema 22 authenticates schema 21, upgrades retained F0 death diagnostics from v1 to v2 with an empty career change set, and preserves the snapshot, authoritative checksum, and source bytes. The exact-F0 schema-21 fixture is 34,795 bytes, has file SHA-256 `8214d8f3430e48a09bc215a2dfc34608924472e44622885250351350a4581686`, and stores checksum `a108ed5ad5932992db2c2adfac6a9987320f3ab2493a2719d4035c05701b39e2`. It was generated through public workflows and `SaveStore` in a temporary detached worktree at exact F0 `783ccfb61357248158cf287ee69ba27b56c38f4a`; the generator was removed.

The 40-case focused slice covers all proposal and service roles, retained-retinue preservation, retention folding/overflow, exact affected IDs and tamper rollback, 16 priority/submission-order race executions, shared-record simultaneous deaths, later-day replay, schema migration/injection rejection, and current pending/resolved saves. The raw 1,000-character/200-death career-rich fixture measured 5,940.911 ms workflow, 0.693 ms query, and 4.326 ms snapshot/checksum on local Apple Silicon, with 1,441,251 JSON bytes, 85,205 gzip bytes, and checksum `e8da6e52e9b1c34d0c2cd6787f5aa65f9564cca28f2ecf1415db1568d1a9f730`. This is not the full SP-04 performance gate. Exact-SHA hosted evidence remains pending, so F1 is not yet accepted. Inheritance, heir designation, claims, regency, offices/titles, disputed succession, household-head/captive disposition, player transfer, content, UI, AI, and battle remain pending.
