# Build Metadata

`toolchain.json` records exact engine/template/SDK expectations. `version.json` is the source of the distributable project version. Both feed the generated build manifest; update them only through a reviewed toolchain or release change.
