# Development Mods

Place data-only development packs in child directories, each with a `content-manifest.json`. This directory is not a runtime scripting or assembly-loading boundary. Shipped built-in content remains rooted at `data/content-manifest.json`.
